from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Base image recognition model for land use based on Sentinel2 satellite images.',
    author='Denise Sengül',
    license='MIT',
)
